---
export interface Props {
  id: string;
  placeholder: string;
  rows?: number;
}

const { id, placeholder, rows } = Astro.props as Props;
---

<div
  class={`
    block
    text-center
  `}
>
  <span
    class={`
      block
      font-bold
      py-1
      text-shadow
    `}
  >
    <slot />
  </span>
  <textarea
    class={`
      backdrop-blur
      bg-white-alpha-70
      border
      border-white-alpha-20
      dark:bg-dark-card
      dark:placeholder-white-alpha-40
      dark:text-white
      dark:text-white
      max-w
      p-2
      placeholder-black-alpha-40
      rounded-md
      text-black
      w-full
    `}
    id={id}
    placeholder={placeholder}
    rows={rows}></textarea>
</div>
<style>
  span {
    text-shadow: 0px 0px 4px rgba(0, 0, 0, 1);
  }
</style>
